- if user_signed_in?


  %li.dropdown
    %a.dropdown-toggle{"data-toggle" => "dropdown", :href => "#"}
      = current_user.email
      %span.caret

    %ul.dropdown-menu{:role => "menu"}
      %li
        = link_to 'Logout', destroy_user_session_path, :method => :delete
      %li
        = link_to 'User settings', root_path



- else
  %li
    = link_to('Login', new_user_session_path)